import React,{useState, useEffect} from "react";

const CRUD = () => {
    const empdata =[
        {
            
        }
    ]
    const [data, setData] = useEffect([]);
    return(
        <div>CRUD</div>
    )
}

export default CRUD;